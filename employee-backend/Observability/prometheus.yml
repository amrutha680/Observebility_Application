global:
  scrape_interval: 5s     # Prometheus pulls metrics every 5 seconds

scrape_configs:
  - job_name: "otel-collector-metrics"
    static_configs:
      - targets: ["otel-collector:9314"]
        # If your app runs on localhost:8080, use this inside Docker
